import React from 'react';
import ReactDOM from 'react-dom';
import './assets/scss/setting/index.scss';  // 변경된 부분
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
